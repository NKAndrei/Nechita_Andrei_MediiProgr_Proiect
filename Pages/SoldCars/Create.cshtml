@page
@model Nechita_Andrei_MediiProgr_Proiect.Pages.SoldCars.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>SoldCar</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <!--
    <div class="form-group">

        {
            foreach (var car in Model.CarList)
            {
                <tr>car.Model.Name</tr>
            }
        }

    </div>
    <div class="form-group">
        {
            foreach (var car in Model.CarList)
            {
                <tr>car.Make.Name</tr>
            }
        }
        -->
            <p>@Model.isCarListNull</p>
            <p>@Model.isCustomerListNull</p>
            <div class="form-group">
                @{
                    if (Model.CarList != null && Model.CustomerList != null)
                    {
                        <p>Customers</p>
                        @Html.DropDownList("CarStringID", new SelectList(Model.CarList, "ID", "Description"));
                        @Html.DropDownList("CustomerStringID", new SelectList(Model.CustomerList, "ID", "Name"));
                    }
                }

            </div>

            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}

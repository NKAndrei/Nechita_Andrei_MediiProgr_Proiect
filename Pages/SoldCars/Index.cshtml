@page
@model Nechita_Andrei_MediiProgr_Proiect.Pages.SoldCars.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SoldCars[0].Car.Description)
                @Html.DisplayNameFor(model => model.SoldCars[0].Car.Price)
                @Html.DisplayNameFor(model => model.SoldCars[0].Customer.Name)
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.SoldCars) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Car.Description)
                @Html.DisplayFor(modelItem => item.Car.Price)
                @Html.DisplayFor(modelItem => item.Customer.Name)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
